# TrendSoccer 유튜브 숏폼 — 스토리라인 설계서

**작성일**: 2026-08-20
**대상 채널**: 유튜브 쇼츠
**목적**: AI 예측 안내 + trendsoccer.com 유입
**종목**: 축구 / 야구 — **종목별 개별 영상**으로 분리 제작

---

## 편성 개요

| 포맷 | 주기 | 길이 | 목적 |
|---|---|---|---|
| **A. 데일리 픽 리포트** | 매일 (종목별 각 1개) | 20s | 오늘 경기 안내 · 유입 |
| **B. 어제 성적표** | 매일 (종목별 각 1개) | 17s | 신뢰 구축 |
| **D. 원 매치 딥다이브** | 주 1~2회 (빅매치만) | 21.5s | 깊이 · 검색 유입 |
| **E. 주말 TOP 5** | 주 1회 (금요일) | 22s | 주말 트래픽 |

> 수시 트리거 포맷(배당 급변 알림)은 이번 범위에서 제외.

### 주간 편성

| | 월~목 | 금 | 토·일 |
|---|---|---|---|
| 오전 | B (어제 성적표) | B | B |
| 오후 | A (오늘의 픽) | A + **E** | A |
| 추가 | — | — | **D** (빅매치 있을 때) |

D 는 이미 구현 완료(v2). 새로 만들 것은 **A, B, E** 세 개.

---

## 포맷 A — 데일리 픽 리포트

> **한 줄 정의**: "오늘 N경기를 분석했고, 그중 3개만 통과했습니다."

### 왜 이 구조인가

단순히 "오늘의 픽 3개"라고 하면 광고로 읽힌다.
**"18경기를 걸러서 3개만 남았다"** 는 필터링 서사를 앞에 두면
같은 정보가 선별의 결과물로 읽히고, 픽 하나하나의 무게가 달라진다.

### 씬 구성 (총 20.0s / 1200f @ 60fps)

| # | 씬 | 길이 | 내용 |
|---|---|---|---|
| 1 | `opener` | 2.5s (150f) | 날짜 + 종목 + **필터링 카운터** |
| 2 | `pick1` | 4.0s (240f) | 첫 번째 픽 |
| 3 | `pick2` | 4.0s (240f) | 두 번째 픽 |
| 4 | `pick3` | 4.0s (240f) | 세 번째 픽 |
| 5 | `summary` | 3.0s (180f) | 3경기 한 화면 요약 |
| 6 | `cta` | 2.5s (150f) | 사이트 유도 |

### 씬별 연출

**1. `opener` — 필터링 서사**

```
        2026. 08. 20  목요일
      ┌──────────────────────┐
      │   오늘 분석한 경기    │
      │        1 8 경기       │   ← 숫자 카운트업
      │          ↓            │
      │   AI 통과: 3 경기     │   ← 필터 통과 애니메이션
      └──────────────────────┘
           ⚽ 유럽 축구
```

- 0f: 날짜 페이드인
- 12f: "오늘 분석한 경기" + 총 경기수 카운트업 (0 → 18)
- 60f: 아래 화살표 + 회색 점들이 우수수 떨어지고 3개만 남는 연출
- 85f: "AI 통과: 3경기" 팝 + 브랜드 글로우

**2~4. `pick1~3` — 픽 카드**

```
   ①                    프리미어리그
   ┌──────────────────────────────┐
   │  [크레스트]   vs   [크레스트]  │
   │   맨시티            리버풀     │
   │                              │
   │  ████████████░░░░  68%       │  ← 바 채워짐
   │  ★★★★☆                       │
   │  오늘 오후 11:30              │
   └──────────────────────────────┘
```

- 0f: 카드 아래에서 슬라이드 인 + 번호(①②③) 팝
- 10f: 양팀 크레스트 팝
- 30f: 승률 바 0 → 68% 채워짐, 숫자 카운트업
- 55f: 별점 순차 팝
- 75f: 경기 시각 페이드
- **누적감**: 이전 픽 카드는 화면 위쪽에 작게 축소되어 남는다.
  3번째 씬에서는 위에 ①② 미니 카드 두 장이 보인다.

**5. `summary` — 스크린샷 유도**

3경기를 한 화면에 압축. 시청자가 캡처해 갈 수 있게.
"저장해두세요" 같은 문구 없이도 캡처 유도가 되는 레이아웃.

**6. `cta`**

```
      나머지 15경기 분석과
      실시간 배당 흐름은

      trendsoccer.com
```

> 유입 문구는 "나머지 N경기"로 **구체 숫자**를 쓴다.
> "더 보기"보다 클릭률이 높다.

### 데이터 소스

| 항목 | 출처 |
|---|---|
| 축구 픽 | `/api/pick-recommendations` (오늘 날짜) |
| 축구 총 경기수 | `/api/odds-from-db?league=ALL` 카운트 |
| 야구 픽 | `/api/admin/shorts-data?league=KBO\|NPB\|MLB` |
| 리그명·팀 컬러 | 기존 매핑 재사용 |

### 픽 선정 기준 (제안)

승률 상위 3개를 그냥 뽑으면 매일 비슷한 강팀만 나온다.
다음 순으로 거르는 걸 권장:

1. 별점 4 이상 (또는 승률 60% 이상)
2. 서로 다른 리그에서 최소 2개 (다양성)
3. 그중 승률 순 상위 3개

---

## 포맷 B — 어제 성적표

> **한 줄 정의**: "어제 5경기 예측했고, 4개 맞고 1개 틀렸습니다."

### 왜 이 구조인가

예측 채널의 신뢰는 전부 "지난 예측이 맞았나"에서 나온다.
**틀린 것을 그대로 보여주는 게 신뢰를 만든다.**
맞은 것만 골라 보여주는 채널은 시청자가 금방 알아챈다.

### 씬 구성 (총 17.0s / 1020f)

| # | 씬 | 길이 | 내용 |
|---|---|---|---|
| 1 | `hook` | 2.5s (150f) | "어제 5경기, 결과는?" + 뒤집힌 카드들 |
| 2 | `results` | 8.0s (480f) | 경기당 1.6s, 카드 순차 오픈 |
| 3 | `score` | 3.5s (210f) | 적중 카운트 + 누적 적중률 |
| 4 | `cta` | 3.0s (180f) | 오늘 픽 유도 |

### 씬별 연출

**1. `hook` — 카드백**

```
      어제 AI가 예측한
         5 경기

     ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐
     │?│ │?│ │?│ │?│ │?│     ← 뒷면
     └─┘ └─┘ └─┘ └─┘ └─┘

        결과는?
```

**2. `results` — 순차 오픈**

경기당 96프레임(1.6s). 카드가 Y축 회전으로 뒤집힌다.

```
  ①  ┌────────────────────────────┐
     │ 프리미어리그               │
     │ 맨시티  2 : 1  리버풀      │
     │ AI 예측: 맨시티 승  ✅     │  ← 초록 글로우
     └────────────────────────────┘
```

- 0f: 카드 flip 시작 (rotateY 180° → 0°)
- 20f: 내용 노출, 스코어 카운트업
- 35f: ✅ 또는 ❌ 도장 찍히듯 팝
  - ✅ 초록 글로우 + 살짝 스케일 바운스
  - ❌ 회색 + 채도 낮춤, 흔들림 없음 (조롱하지 않는 톤)
- 다음 카드는 위로 스택

**3. `score` — 집계**

```
        5경기 중

        4 적중
     ████████░░  80%           ← 카운트업

    최근 30일 누적  67%
    (프리미어리그 71% · 라리가 64%)
```

- 누적 적중률은 `pick-accuracy` 그대로
- 리그별 상위 2개를 괄호로 병기 → 데이터의 두께를 보여준다

**4. `cta`**

```
      오늘 예측도
      이미 올라와 있습니다

      trendsoccer.com
```

### 데이터 소스

| 항목 | 출처 | 비고 |
|---|---|---|
| 어제 경기별 결과 | `pick_recommendations` | `settled_at` 있고 `commence_time` 이 어제 |
| 픽 / 실제 결과 | `pick_result`, `actual_result` | HOME/DRAW/AWAY |
| 스코어 | `final_score_home`, `final_score_away` | |
| 적중 여부 | `is_correct` | |
| 누적 적중률 | `/api/pick-accuracy` | 리그별 + 전체 |

**필요 작업**: `/api/pick-recommendations` 가 이미 `commence_time` 범위 필터를 지원하므로,
어제 날짜 + `is_correct is not null` 조건만 얹으면 된다. 새 테이블·새 크론 불필요.

### 열린 이슈

- **야구 픽도 `pick_recommendations` 에 쌓이는지 확인 필요.**
  축구 전용이면 야구 성적표는 별도 소스(`baseball_matches` 정산 컬럼)가 필요하다.
- 경기 수가 5개 미만인 날 → 카드 개수에 맞춰 `results` 씬 길이 자동 조정
- 정산된 픽이 0건인 날 → 영상 생성 스킵 (렌더 스크립트에서 처리)

---

## 포맷 E — 주말 TOP 5 (금요일)

> **한 줄 정의**: "이번 주말, AI가 가장 확신한 5경기를 5위부터 공개합니다."

카운트다운은 끝까지 보게 만드는 검증된 구조다.

### 씬 구성 (총 22.0s / 1320f)

| # | 씬 | 길이 | 내용 |
|---|---|---|---|
| 1 | `opener` | 2.5s | "이번 주말 TOP 5" |
| 2 | `rank5` | 3.0s | 5위 |
| 3 | `rank4` | 3.0s | 4위 |
| 4 | `rank3` | 3.0s | 3위 |
| 5 | `rank2` | 3.0s | 2위 |
| 6 | `rank1` | 4.5s | **1위** — 연출 강화 |
| 7 | `cta` | 3.0s | 사이트 유도 |

- 순위 숫자를 화면 배경에 크게 워터마크로 깔고, 그 위에 경기 카드
- 1위는 카드가 커지고 글로우 + 별점 5개 강조
- A 포맷의 픽 카드 컴포넌트를 그대로 재사용 → 추가 작업 최소

---

## 공통 규격

| 항목 | 값 |
|---|---|
| 해상도 | 1080 × 1920 (9:16) |
| 프레임레이트 | 60fps |
| 인코딩 | H.264, crf 18, preset slow |
| 상단 | 워드마크 + 종목/리그 뱃지 + 진행바 |
| 하단 | trendsoccer.com (쇼츠 UI 안전영역 위 250px) |
| 배경 | Dreamina(Seedance) 9:16 루프 · 없으면 그라디언트 폴백 |
| 폰트 | Pretendard (번들 인라인) |

### 유튜브 정책 주의

- **배당(오즈) 수치를 전면에 내세우지 않는다.** "AI 예측 승률"로만 표기
- "베팅", "배팅", "적중 시 수익" 류 표현 금지
- 채널 포지셔닝은 **스포츠 데이터 분석**
- 포맷 B 의 "적중률"은 예측 정확도(accuracy)로 표현 — 수익률 아님

---

## 재사용 컴포넌트 계획

A · B · E 가 공유하는 것:

```
components/
  PickCard.tsx      팀 vs 팀 + 승률 바 + 별점   → A, E
  ResultCard.tsx    스코어 + 픽 + ✅/❌          → B
  CardStack.tsx     이전 카드가 위로 축소·누적   → A, B, E
  BigNumber.tsx     카운트업 큰 숫자             → A, B
  Chrome.tsx        상·하단 고정 요소 (기존)     → 전부
  Background.tsx    Dreamina 배경 (기존)         → 전부
```

`PickCard` 와 `CardStack` 만 만들면 A 와 E 는 사실상 같은 부품으로 조립된다.
그래서 **A → B → E** 순으로 만들면 뒤로 갈수록 작업량이 준다.

---

## 다음 단계

1. `PickCard` / `ResultCard` / `CardStack` 컴포넌트 작성
2. 포맷 A 조립 → 축구·야구 각각 렌더 확인
3. 포맷 B 조립 → 야구 픽 데이터 소스 확인 후
4. 포맷 E 조립 (A 부품 재사용)
5. 렌더 스크립트에 `--format=daily|result|top5` 옵션 추가
6. GitHub Actions 로 일간 자동 렌더

---

# 부록 — 실측 적중률 (2026-08-20 확인)

`/api/pick-accuracy` 조회 결과. 정산 완료 픽 1,000건 기준.

## 전체

**54%** (540 / 1000)

## 리그별

| 리그 | 표본 | 적중률 | 판단 |
|---|---|---|---|
| 세리에A | 136 | **60%** | 픽 대상 |
| 라리가 | 150 | 56% | 픽 대상 |
| 프리미어리그 | 135 | 56% | 픽 대상 |
| 분데스리가 | 105 | 56% | 픽 대상 |
| 리그1 | 127 | 56% | 픽 대상 |
| 에레디비시 | 104 | 56% | 픽 대상 |
| 챔피언스리그 | 33 | 45% | 표본 부족, 관찰 |
| J1리그 | 68 | 43% | **모델 부진** |
| MLS | 5 | 40% | 표본 부족 |
| K리그2 | 10 | 40% | 표본 부족 |
| **K리그1** | **42** | **31%** | **모델 부진 — 랜덤(33%)보다 낮음** |

> 표본 10건 미만 리그(DFB 3건 100%, ACL 2건 100% 등)는 통계적으로 무의미하므로 제외.

## 해석

- 승/무/패 3-way 예측에서 랜덤은 33%, 북메이커 기준선은 대략 50~53%.
  **유럽 5대 리그의 56~60%는 표본 100건 이상에서 의미 있는 수치**다.
- **K리그1 31%는 별개의 문제다.** 42경기 표본에서 랜덤보다 낮은 건
  우연으로 설명하기 어렵다. 유럽 리그 기준으로 만들어진 로직이
  K리그 특성(홈 어드밴티지 구조, 스플릿 라운드, 팀 간 전력 편차)에
  맞지 않을 가능성이 크다. **콘텐츠와 별개로 알고리즘 점검 필요.**

## 콘텐츠 반영 결정

1. **포맷 A 픽 대상**: 유럽 5대 리그 + 챔스/유로파/컨퍼런스 (`group=euro`)
2. **K리그는 별도 영상으로 분리** (`group=kleague`)
   - 국내 시청자 도달을 위해 만들되, 유럽 리그와 한 영상에 섞지 않는다.
   - 섞으면 포맷 B(성적표)에서 유럽 리그 신뢰도까지 같이 깎인다.
3. **포맷 B 적중률 표기**: 전체 54%가 아니라 **5대 리그 한정 수치**를 쓰고,
   화면에 기준 범위를 명시한다. 전체를 숨기고 높은 숫자만 쓰면 체리피킹이지만,
   기준을 밝히면 정확한 진술이다.

## 미확인 사항

무승부를 제외한 승/패 2-way 적중률. 축구 3-way 에서 무승부는 사실상
맞추기 어려운 영역이라, 제외하면 수치가 크게 오를 수 있다.
(야구 API 는 이미 동점 제외 2-way 로 계산한다)

```sql
select
  case when actual_result = 'DRAW' then '무승부 발생' else '승패 결정' end as 구분,
  count(*) as 경기수,
  count(*) filter (where is_correct) as 적중,
  round(100.0 * count(*) filter (where is_correct) / count(*)) as 적중률
from pick_recommendations
where is_correct is not null
  and league_code in ('PL','PD','SA','BL1','FL1','DED','CL','EL')
group by 1;
```

65% 이상 나오면 포맷 B 를 "승패 예측 적중률" 로 프레이밍하는 게 가장 강력하다.

---

# 진행 상황 (2026-08-20)

| 포맷 | 상태 |
|---|---|
| A 데일리 픽 | ✅ 구현 완료 (`DailyPicks` 컴포지션 + `/api/admin/shorts-daily`) |
| B 어제 성적표 | ⬜ 미착수 — 적중률 프레이밍 확정 후 |
| D 빅매치 딥다이브 | ✅ 구현 완료 (`Shorts` 컴포지션) |
| E 주말 TOP 5 | ⬜ 미착수 — A 의 PickCard 재사용 예정 |

## 구현 중 발견한 것

**외부 이미지를 렌더에 직접 물리면 안 된다.**
팀 로고를 API-Football URL 그대로 `<Img>` 에 넘겼더니 렌더가 12배 느려졌다
(111프레임 3분). Remotion 은 프레임마다 컴포넌트를 다시 마운트하므로
죽은 URL 을 1,200번 재요청한다. 해결:

1. 렌더 스크립트가 실행 전에 로고를 받아 **data URI 로 치환** (`inlineLogos`)
2. 컴포넌트에 모듈 레벨 실패 URL 캐시 추가

적용 후 111프레임 23초. 폰트를 인라인한 것과 같은 원리다.

---

# 부록 2 — 무승부 제외 적중률 (2026-08-20 확인)

## 핵심 발견

**모델은 무승부를 전혀 예측하지 않는다.**

| 구분 | 경기수 | 적중 | 적중률 |
|---|---|---|---|
| 승패 결정 | 631 | 456 | **72%** |
| 무승부 발생 | 176 | 0 | **0%** |

807경기 전부에서 HOME 아니면 AWAY 만 찍는다.
축구는 구조적으로 22~25% 가 무승부로 끝나므로,
**경기의 약 5분의 1은 시작부터 자동 오답**이다.

전체 3-way 적중률 54% 가 낮았던 진짜 이유가 이것이다.
예측력이 부족한 게 아니라 무승부를 포기하고 있었다.

## 리그별 (표본 40경기 이상)

| 리그 | 전체 | 무승부율 | 승패 기준 적중률 | 판단 |
|---|---|---|---|---|
| 세리에A | 136 | 21% | **77%** | 픽 대상 |
| 에레디비시 | 108 | 28% | **77%** | 픽 대상 |
| 리그1 | 127 | 22% | 72% | 픽 대상 |
| 프리미어리그 | 135 | 22% | 72% | 픽 대상 |
| 분데스리가 | 105 | 22% | 72% | 픽 대상 |
| 라리가 | 153 | 18% | 70% | 픽 대상 |
| J1리그 | 74 | 30% | 62% | 제외 |
| **K리그1** | 48 | 21% | **42%** | **제외** |

**유럽 6개 리그 합산: 승패 결정 596경기 기준 73%**

## K리그 재해석

앞서 K리그1 의 3-way 적중률 31% 를 두고 모델 부진을 의심했는데,
무승부를 제외해도 **42%** 로 여전히 낮다.
무승부율은 21% 로 유럽과 동일하므로 **무승부가 원인이 아니다.**

다만 승패가 갈린 K리그 경기는 38경기뿐이라,
통계적으로는 동전 던지기(50%)와 유의하게 구분되지 않는다.
정확한 표현은 **"현재 데이터로는 K리그에서 예측력이 확인되지 않는다"** 이며,
"체계적으로 반대로 예측한다" 고 단정할 표본은 아니다.

어느 쪽이든 유럽(70~77%)과는 명백히 다른 구간이므로 픽 대상에서 제외한다.
표본이 100경기를 넘으면 재평가할 것.

---

# 알고리즘 개선 과제 (숏폼과 별개)

## 1. 무승부 예측 부재 ⭐ 최우선

현재 `pick_result` 는 HOME / AWAY 만 출력한다.
무승부 176경기가 전부 자동 오답으로 처리되고 있다.

- 에레디비시는 무승부율 28%, 승패 기준 77% 로 최상위인데
  3-way 전체로는 56% 까지 떨어진다. 순전히 무승부 때문이다.
- 무승부 확률이 일정 임계값을 넘으면 DRAW 를 픽하도록 바꾸거나,
  최소한 "무승부 위험 높음" 플래그를 붙여 픽에서 제외하는 것만으로도
  전체 적중률이 크게 오른다.

## 2. K리그 모델

유럽 리그 기준으로 만들어진 로직이 K리그에 맞지 않는다.
홈 어드밴티지 구조, 스플릿 라운드, 팀 간 전력 편차 등을 점검할 것.

---

# 포맷 B 적중률 표기 (확정)

화면에는 **기준을 반드시 병기**한다.

```
      승패가 갈린 경기 기준

           73%
      ████████████░░░░

    유럽 6개 리그 · 최근 596경기
```

- `73%` 만 크게 띄우고 기준을 빼면 체리피킹이다.
- 기준(승패 결정 / 유럽 6개 리그 / 표본 수)을 같이 쓰면 정확한 진술이다.
- 무승부로 끝난 경기는 성적표에서 ❌ 가 아니라
  **회색 "무" 로 따로 표시**해 예측 대상이 아니었음을 드러낸다.
