[phases.setup]
nixPkgs = ['python39']

[phases.install]
cmds = ['cd python-api && pip install -r requirements.txt']

[start]
cmd = 'cd python-api && uvicorn baseball_api_server:app --host 0.0.0.0 --port $PORT'
